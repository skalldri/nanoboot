//#include "nrf_gpio.h"
//#include "nrf_delay.h"

//#define LED_PIN 17

int main()
{
    //nrf_gpio_cfg_output(LED_PIN);

    while(true)
    {
        //nrf_gpio_pin_set(LED_PIN);

        //nrf_delay_ms(1000);

        //nrf_gpio_pin_clear(LED_PIN);

        //nrf_delay_ms(1000);
    }

    return 0;
}